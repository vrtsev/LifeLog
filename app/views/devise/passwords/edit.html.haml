%h4 Изменение пароля

= form_for resource, as: resource_name, url: password_path(resource_name), html: { method: :put, class: 'ui large form' } do |f|
  = f.hidden_field :reset_password_token

  .ui.stacked.segment
    .field= f.password_field :password, placeholder: "Новый пароль (минимум #{@minimum_password_length} символов)", autofocus: true, autocomplete: "off"
    .field= f.password_field :password_confirmation, placeholder: "Подтверждение пароля", autocomplete: "off"
    
    .actions= f.submit "Изменить пароль", class: 'ui fluid large teal submit button'

  .additional.links
    %p= link_to 'Войти?', new_session_path(resource_name)
    %p== Вы еще не зарегистрированы? #{link_to 'Зарегистрироваться', new_registration_path(resource_name)}
      
