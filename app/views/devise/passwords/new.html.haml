%h4 Восстановление пароля

= form_for resource, as: resource_name, url: password_path(resource_name), html: { method: :post, class: 'ui large form' } do |f|
  .ui.stacked.segment
    .field
      .ui.left.icon.input
        %i.user.icon
        = f.email_field :email, placeholder: 'Электронная почта', autofocus: true
    
    %p Вы получите письмо на почту с инструкцией по восстановлению пароля

    .actions
      = f.submit "Восстановить", class: 'ui fluid large teal submit button'

  .additional.links
    %p= link_to 'Войти?', new_session_path(resource_name)
    %p== Вы еще не зарегистрированы? #{link_to 'Зарегистрироваться', new_registration_path(resource_name)}
      
