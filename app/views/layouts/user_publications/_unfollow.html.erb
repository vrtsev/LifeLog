<%= form_for current_user, url: user_follow_path(@user, @user), html: { method: :delete }, remote: true do |f| %>
  <%= f.submit "Отписаться", class: "btn btn-sm rounded danger w-sm m-y-xs" %>
<% end %>