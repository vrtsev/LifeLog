<%= form_for current_user, url: user_follows_path(@user), remote: true, method: :post do |f| %>
  <div><%= hidden_field_tag :followed_id, @user.id %></div>
  <%= f.submit "Подписаться", class: "btn btn-sm rounded primary w-sm m-y-xs" %>
<% end %>