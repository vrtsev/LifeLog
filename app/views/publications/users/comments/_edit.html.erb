<%= form_for :comment, url: user_post_comment_path(@user, @post, comment), method: :patch do |f| %>
  <%= f.text_area :content, rows: 2, class: 'form-control' %>
  <br>
  <%= f.submit 'Отправить', class: 'pull-right btn btn-sm btn-primary' %>&nbsp;
<% end %>
