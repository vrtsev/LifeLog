$('#posts').append("<%= j render partial: 'post', locals: { posts: @posts.unpinned } %>");

<% if @posts.next_page %>
  $('.pagination').replaceWith("<%= j will_paginate @posts %>");
<% else %>
  $('.pagination').remove();
<% end %>
