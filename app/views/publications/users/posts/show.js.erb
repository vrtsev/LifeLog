$('#comments').append("<%= j render partial: 'publications/users/posts/ajax_comments', locals: { comments: @comments } %>");

<% if @comments.next_page %>
  $('.pagination').replaceWith("<%= j will_paginate @comments %>");
<% else %>
  $('.pagination').remove();
<% end %>
