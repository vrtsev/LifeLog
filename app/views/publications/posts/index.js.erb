$('#posts').append('<%= j concept("publications/post/cell/index_post_block", collection: @posts.unpinned).to_s %>');

<% if @posts.next_page %>
  $('.pagination').replaceWith("<%= j will_paginate @posts %>");
<% else %>
  $('.pagination').remove();
<% end %>
