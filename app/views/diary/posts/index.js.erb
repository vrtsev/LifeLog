$('#posts').append("<%= j( render partial: 'pagination_posts', locals: { posts: @posts.unpinned }) %>");

<% if @posts.next_page %>
  $('.pagination').replaceWith("<%= j will_paginate @posts %>");
<% else %>
  $('.pagination').remove();
<% end %>
