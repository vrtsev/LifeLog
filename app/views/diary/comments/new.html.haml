%h1 New DIARY comment

= form_for :comment, url: diary_post_comments_path(@post), method: :post do |f|
  - if @comment.errors.any?
    #error_explanation
      %h2= "#{pluralize(@comment.errors.count, "error")} prohibited this comment from being saved:"
      %ul
        - @comment.errors.full_messages.each do |message|
          %li= message

  .field
    = f.label :content
    = f.text_area :content
  .field
    = f.label :post_id
    = f.number_field :post_id
  .field
    = f.label :user_id
    = f.number_field :user_id
  .field
    = f.label :type
    = f.text_field :type
  .actions
    = f.submit 'Save'


= link_to 'Back', diary_post_path(@post)
